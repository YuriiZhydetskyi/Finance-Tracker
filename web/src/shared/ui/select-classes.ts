// Shared Tailwind classes for native <select>. Mirrors <Input> sizing/border so
// selects in forms and filter bars line up visually with text inputs.
export const SELECT_CLASS =
  'flex h-10 w-full rounded-md border border-slate-300 bg-white px-2 text-sm focus:border-slate-900 focus:outline-none focus:ring-2 focus:ring-slate-900';

export const FIELD_LABEL_CLASS = 'text-xs font-medium text-slate-600';

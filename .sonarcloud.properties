# SonarCloud automatic analysis settings.
# supabase/functions/_shared/domain/ is generated from packages/domain by
# scripts/sync-edge-domain.mjs (drift is guarded by `npm run check:edge-domain`).
# Analysing it again only reports the source's issues twice and counts it as
# duplicated code.
sonar.exclusions=supabase/functions/_shared/domain/**

{
  "name": "finance-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Personal finance tracker for two: photo-based receipt parsing via Gemini, Google Sheets as storage, Apps Script web app.",
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "clasp deploy",
    "logs": "clasp logs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
